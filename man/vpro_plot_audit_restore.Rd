% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-crud.R
\name{vpro_plot_audit_restore}
\alias{vpro_plot_audit_restore}
\title{Restore one audited plot field}
\usage{
vpro_plot_audit_restore(context, plot_number, audit, delete_audit = FALSE)
}
\arguments{
\item{context}{A VPRO project context with an active project.}

\item{plot_number}{Plot identifier.}

\item{audit}{Complete one-row event selected from \code{\link[=vpro_plot_audit_list]{vpro_plot_audit_list()}}.}

\item{delete_audit}{Whether to delete the successfully restored audit event in
the same transaction. Defaults to \code{FALSE}.}
}
\value{
A list containing the restored target row and audit event, invisibly.
}
\description{
Restores the \code{BeforeEdit} value recorded by one canonical audit event. The
complete one-row event returned by \code{\link[=vpro_plot_audit_list]{vpro_plot_audit_list()}} is required so its
contents can be reverified before mutation; SQLite row IDs alone are not
durable event identities. \verb{_Env} fields are resolved against both physical
Env and Admin schemas, while child fields require exactly one
\verb{(PlotNumber, ID)} row. The current stored value must still equal the event's
\code{AfterEdit} value, preventing an older event from silently overwriting later
work.
}
\details{
Restoration changes exactly one existing field in one SQLite transaction. It
does not create missing child rows, delete empty vegetation rows, or append a
second audit event. The restored audit row is preserved unless
\code{delete_audit = TRUE} is explicitly requested.
}
