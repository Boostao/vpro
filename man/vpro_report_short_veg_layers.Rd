% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-short-veg-layers.R
\name{vpro_report_short_veg_layers}
\alias{vpro_report_short_veg_layers}
\title{Return bounded short-vegetation layer data for the active site unit}
\usage{
vpro_report_short_veg_layers(
  context,
  mapping_path = vpro_bundled_file("extdata", "short-veg-layer-mapping-v1.sqlite"),
  enforce_quality = FALSE,
  reference_path = vpro_bundled_file("extdata", "VLists.db"),
  site_min = "Poor",
  veg_min = "Poor",
  soil_min = "Poor",
  include_site_missing = TRUE,
  include_veg_missing = TRUE,
  include_soil_missing = TRUE,
  bec_min = NULL,
  include_bec_missing = TRUE
)
}
\arguments{
\item{context}{VPRO context with active project and SU.}

\item{mapping_path}{Versioned SQLite mapping; defaults to the bundled copy.}

\item{enforce_quality}{Apply report quality criteria; defaults to \code{FALSE}.}

\item{reference_path}{Bundled VLists SQLite database by default, used only
when quality is enforced.}

\item{site_min, veg_min, soil_min}{Inclusive quality thresholds.}

\item{include_site_missing, include_veg_missing, include_soil_missing}{Whether
missing/unrecognized qualities pass their respective criteria.}

\item{bec_min}{Optional lexical BEC minimum; \code{NULL} disables BEC filtering.}

\item{include_bec_missing}{Whether missing BEC passes when BEC is enabled.}
}
\value{
A list containing \code{rows} (SiteUnit, PlotNumber, Species, layer,
layer_code, strata, numeric cover), \code{units} (SiteUnit, integer n_plots
counting unique eligible SU plot memberships including orphans), and
\code{diagnostics} (mapping_version, quality_applied, quality_removed,
quality_thresholds, excluded_null_or_blank_plot_rows,
excluded_null_or_blank_unit_rows, duplicate_membership_rows and
eligible_memberships). Duplicate count is excess rows per SU/plot pair.
}
\description{
Uses physical SU membership and physical project Veg, not metadata joins or
report views. Repeated Veg records for the same plot and species are reduced
by independent \code{MAX} for each of the ten supported cover columns. Non-null
zero is retained. Report keys join the versioned \code{Layer1234567} mapping (not
the padded \code{LayerCode} key). This is layer data, not an Access summary or a
taxonomic determination; species values are left unchanged.
}
\details{
Null and whitespace-only PlotNumber or SiteUnit memberships are ineligible
and counted separately. Other orphan plot memberships remain in the unit
denominators when quality is disabled. When enabled, quality filtering uses
\code{\link[=vpro_filter_plot_quality]{vpro_filter_plot_quality()}} and can remove orphans through its Project
criterion. No source SQLite database is modified.
}
