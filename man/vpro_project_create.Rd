% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-create.R
\name{vpro_project_create}
\alias{vpro_project_create}
\title{Create an empty VP08 project with initial reference-version audit rows}
\usage{
vpro_project_create(
  path,
  project,
  user,
  reference_path = vpro_bundled_file("extdata", "VLists.db"),
  template_path = vpro_bundled_file("extdata", "projects", "Sample.db")
)
}
\arguments{
\item{path}{Existing SQLite file or a path for a new file.}

\item{project}{New project name, excluding the reserved \code{Sample} name.}

\item{user}{Nonblank user name recorded in the three audit rows.}

\item{reference_path}{SQLite list reference with \code{USysAllSpecs} and
\code{USysTableOfLists}, defaulting to bundled \code{VLists.db}.}

\item{template_path}{SQLite VP08 project schema source, defaulting to the
bundled Sample project. Only table definitions, indexes, and metadata
are copied; no rows are copied.}
}
\value{
Normalized target path, invisibly.
}
\description{
Translates \code{V7mdlCreateTables.CreateTableSet} and its call to
\code{V7mdlAudit.LogNewProject}. Copies only the eight \emph{schemas} and indexes of
the bundled VP08 Sample project, not its plot data or audit history.
Creates the target tables, their translated table descriptions, and three
initial audit rows (\code{NewProject}, \code{USysAllSpecs}, \code{USysTableOfLists}) in one
SQLite transaction. Missing reference descriptions are recorded as
\code{"Unknown"}. No project is attached or activated and no Shiny session is
needed. Save-as remains a distinct operation that preserves audit history.
}
