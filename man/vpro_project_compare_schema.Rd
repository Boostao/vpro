% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-schema.R
\name{vpro_project_compare_schema}
\alias{vpro_project_compare_schema}
\title{Compare a VPRO project with a schema template}
\usage{
vpro_project_compare_schema(
  path,
  project,
  template_path = vpro_bundled_file("extdata", "projects", "Sample.db"),
  template_project = "Sample"
)
}
\arguments{
\item{path}{Path to the SQLite database containing the project to check.}

\item{project}{Project prefix to check.}

\item{template_path}{Path to a SQLite database containing the template
project. Defaults to the bundled Sample database.}

\item{template_project}{Project prefix in \code{template_path}.}
}
\value{
A data frame with \code{table}, \code{field}, \code{difference}, \code{expected}, and
\code{actual} columns. A conforming project returns an empty data frame.
}
\description{
Reproduces the field checks in
\code{V7mdlCompareTablesToTemplate.CheckThisProject} for the eight core project
tables. Template fields are checked for presence, normalized declared type,
and declared size. Project-only fields are ignored, matching Access. Missing
project tables are reported explicitly rather than causing a DAO error.
}
\details{
The default template is the bundled VP08 \code{Sample} project. Callers may supply
another SQLite database and project prefix. The comparison is read-only and
returns structured differences instead of opening Excel.
}
