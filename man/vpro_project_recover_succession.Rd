% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-succession-convert.R
\name{vpro_project_recover_succession}
\alias{vpro_project_recover_succession}
\title{Repair an interrupted succession conversion without rewriting Veg}
\usage{
vpro_project_recover_succession(
  path,
  project,
  backup_path,
  authorize,
  context = NULL
)
}
\arguments{
\item{path}{Physical SQLite database, not a packaged resource.}

\item{project}{VP08 project prefix other than Sample.}

\item{backup_path}{New file path in an existing directory for a consistent SQLite backup.}

\item{authorize}{Required callback \code{authorize(permission, resource)};
the permission is \code{convert_succession} or \code{recover_succession}.}

\item{context}{Optional VPRO project context. Supplied contexts must not
have the project database attached.}
}
\value{
A conversion receipt; the year is read from the existing Veg rows.
}
\description{
Only accepts the legacy partial state (Veg year present, Env flag missing)
with one valid nonmissing existing year. Adds the Env flag atomically while
leaving all Veg values untouched. Requires separate authorization and backup.
Other inconsistent states require manual investigation.
}
