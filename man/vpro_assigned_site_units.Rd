% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assigned-site-units.R
\name{vpro_assigned_site_units}
\alias{vpro_assigned_site_units}
\title{List site-unit choices for an active project, SU, or master list}
\usage{
vpro_assigned_site_units(
  context,
  source = c("project", "master", "su"),
  reference_path = vpro_bundled_file("extdata", "VLists.db")
)
}
\arguments{
\item{context}{A VPRO project context. Project and SU scopes require an
active project; SU scope additionally requires an active SU.}

\item{source}{One of \code{"project"}, \code{"master"}, or \code{"su"}, corresponding to
Access selector values 1, 2, and 3.}

\item{reference_path}{SQLite reference database for master choices. Defaults
to the bundled \code{VLists.db}.}
}
\value{
A data frame with \code{UserSiteUnit} for project, \code{SiteUnit} for SU,
or \code{SiteSeries} and \code{SiteSeriesLongName} for master choices.
}
\description{
Returns data for the three concrete SQL branches in
\code{V7mdlTableOfLists.AssignedSiteUnitList}, rather than returning SQL text.
Project choices read the active \code{USysEnv} view (and thus respect active-SU
filtering); SU choices read the physical active SU table; master choices
read \code{MasterSiteUnitList} from an explicit SQLite reference database.
Project and SU results are distinct, omit nulls but retain empty strings,
and are ordered for reproducibility. Master results preserve the Access
\code{Level = 11} filter and \code{SiteSeries} order. Access's fallback branch uses
\code{Name} and \code{UnitLongName}, which are absent from the canonical master list;
unsupported selectors are rejected rather than returning mismatched data.
}
