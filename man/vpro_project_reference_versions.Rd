% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-reference-versions.R
\name{vpro_project_reference_versions}
\alias{vpro_project_reference_versions}
\title{Read reference-list versions recorded in the active project's audit history}
\usage{
vpro_project_reference_versions(context)
}
\arguments{
\item{context}{A VPRO context with an active project.}
}
\value{
A data frame with \code{Table}, \code{Version}, \code{EditWhen} (as stored in
SQLite), \code{Status} (\code{"ok"}, \code{"missing"}, \code{"unknown"}, or \code{"ambiguous"}), and
\code{LatestRows}. An ambiguous version is \code{NA} rather than a guessed value.
}
\description{
Translates \code{V7mdlAllSpecsTools.ProjectVersion} and
\code{V7mdlTableOfLists.ProjectVersionTableOfLists} using the active project's
physical \verb{_Audit} table, not the plot-filtered audit view. For each reference
table, the most recent \code{EditWhen} determines the result; a missing, null, or
empty \code{AfterEdit} becomes \code{"Unknown"} without falling back to older rows.
When more than one event has the latest timestamp, Access does not specify
which wins: the result is marked \code{"ambiguous"} and its version is missing,
even if the tied values agree. Null timestamps rank after non-null timestamps
and are considered tied when no non-null timestamp exists. No audit rows,
project state, or reference-list metadata are modified.
}
