% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-quality.R
\name{vpro_filter_plot_quality}
\alias{vpro_filter_plot_quality}
\title{Filter an active VPRO site unit by report quality}
\usage{
vpro_filter_plot_quality(
  context,
  reference_path = vpro_bundled_file("extdata", "VLists.db"),
  enforce = TRUE,
  site_min = "Poor",
  veg_min = "Poor",
  soil_min = "Poor",
  include_site_missing = TRUE,
  include_veg_missing = TRUE,
  include_soil_missing = TRUE,
  bec_min = NULL,
  include_bec_missing = TRUE
)
}
\arguments{
\item{context}{A VPRO project context with an active project and active SU.}

\item{reference_path}{Path to a SQLite database containing
\code{USysTableOfLists}. Defaults to the bundled VLists database.}

\item{enforce}{Whether to apply quality filtering. If \code{FALSE}, the active SU
is returned unchanged and reference metadata is not consulted.}

\item{site_min, veg_min, soil_min}{Minimum configured \code{DataQuality} labels.}

\item{include_site_missing, include_veg_missing, include_soil_missing}{Whether
missing or unrecognized values pass each quality criterion.}

\item{bec_min}{Optional lexical minimum for \code{BEC_Use}. Use \code{NULL} to omit BEC
filtering, as in the Access long-vegetation report.}

\item{include_bec_missing}{Whether missing \code{BEC_Use} values pass when
\code{bec_min} is supplied.}
}
\value{
A list with deterministic \code{selected} and \code{removed} data frames and a
\code{thresholds} list. Removal diagnostics report \code{Project}, \code{Site}, \code{Veg},
\code{Soil}, \code{BEC}, or \code{Mixed}, plus all failed criteria.
}
\description{
Translates \code{V7mdlReportsQualityControl.QC} and \code{QCLV} without creating
\code{USysDeleteMe_SU} or changing the current plot list. Site, vegetation, and
soil quality labels are ranked by \code{USysTableOfLists} \code{DataQuality} rows.
Thresholds are inclusive. Missing values and labels absent from every list
follow the legacy joined-null branch and are controlled by the corresponding
\verb{include_*_missing} argument. Labels belonging to another list are excluded.
}
\details{
Supplying \code{bec_min} enables the short-report BEC predicate from \code{QC}.
\code{bec_min = NULL} reproduces the long-report \code{QCLV} scope. The BEC comparison
remains a case-insensitive lexical \code{>=} comparison, matching the Access SQL;
it does not use the BEC list's numeric \code{ItemOrder}.
}
