% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-environment.R
\name{vpro_report_environment}
\alias{vpro_report_environment}
\title{Return environmental rows for the active project or site unit}
\usage{
vpro_report_environment(context, plot_numbers = NULL)
}
\arguments{
\item{context}{A VPRO project context with an active project.}

\item{plot_numbers}{Optional character vector of exact plot numbers. Every
value must be non-missing and nonblank; \code{character(0)} returns typed empty
rows.}
}
\value{
A data frame containing every \code{USysEnv} column. Rows are ordered by
\code{PlotNumber}.
}
\description{
Reads every column from the active \code{USysEnv} DuckDB temporary view. The view
supplies the active project scope and, when selected, the active-SU scope.
Rows are ordered by plot number. The existing \code{USysEnv} view removes
duplicate SU-membership rows; order among equal plot numbers is unspecified.
Supplying \code{plot_numbers} restricts the result to exact plot-number matches.
}
\details{
This is a bounded data API only. It does not reproduce EnvReport's Excel
transposition, site-unit worksheet grouping, or RIGHT JOIN handling of SU
rows that have no environmental row. It neither modifies source databases nor
creates report files.
}
