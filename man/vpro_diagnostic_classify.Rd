% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic-classification.R
\name{vpro_diagnostic_classify}
\alias{vpro_diagnostic_classify}
\title{Classify a species across ordered site-unit codes}
\usage{
vpro_diagnostic_classify(unit_codes)
}
\arguments{
\item{unit_codes}{A named character vector in crosstab field order. Each
nonmissing element must have the form \code{"1 - +"} through \code{"5 - 9"};
\code{NA} denotes a missing unit cell. Names must be distinct, nonempty unit
identifiers.}
}
\value{
A list with \code{unit} and \code{diagnosis} (comma-separated codes in Access
order \code{d}, \code{dd}, \code{cd}, \code{c}, \code{ic}). Both are \code{NA_character_} if no diagnosis
would be written.
}
\description{
Translates only the per-species calculation in
\code{V7mdlDiagnostic.Diagnostic}. \code{V7mdlReportsShortVeg.CreateDiagnostic} builds
the \code{MyRS5} crosstab: each unit's code joins
\code{V7mdlDiagnostic.DiagnosticPresenceClass} (1–5) and
\code{V7mdlValueConversions.SignifClass} (\code{+} or 1–9) with \code{" - "}.
The first unit attaining the highest presence is selected. Access uses
\code{Val()} on the first and last characters, so \code{+} counts as zero. With
valid presence classes 1–5, the \code{ic} branch cannot be reached because its
other conditions require a unique near-maximum presence of 2. Unlike
Access, malformed codes and missing unit names fail explicitly. The
function does not build \code{MyRS5}, filter site units, or write reports.
}
