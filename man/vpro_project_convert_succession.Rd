% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-succession-convert.R
\name{vpro_project_convert_succession}
\alias{vpro_project_convert_succession}
\title{Convert a VPRO project to succession data atomically}
\usage{
vpro_project_convert_succession(
  path,
  project,
  year,
  backup_path,
  authorize,
  context = NULL
)
}
\arguments{
\item{path}{Physical SQLite database, not a packaged resource.}

\item{project}{VP08 project prefix other than Sample.}

\item{year}{Whole calendar year from 1 through 9999.}

\item{backup_path}{New file path in an existing directory for a consistent SQLite backup.}

\item{authorize}{Required callback \code{authorize(permission, resource)};
the permission is \code{convert_succession} or \code{recover_succession}.}

\item{context}{Optional VPRO project context. Supplied contexts must not
have the project database attached.}
}
\value{
A receipt with before/after states, row counts, backup path and hash,
and activation status.
}
\description{
Adds the Veg year and Env plot flag in one SQLite transaction. Requires an
explicit authorization callback and a new backup path. If a VPRO context
is supplied the database must be detached from it, and other readers
and writers must be quiesced by the caller. Does not activate the project.
The backup remains available even if conversion rolls back.
}
