% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrity-validation.R
\name{vpro_validate_project_integrity}
\alias{vpro_validate_project_integrity}
\title{Diagnose orphan records and plots without vegetation}
\usage{
vpro_validate_project_integrity(context, use_active_su = TRUE)
}
\arguments{
\item{context}{A VPRO context with an active project.}

\item{use_active_su}{Whether to limit the no-vegetation result through the
active SU, when present. SU orphans are reported whenever an SU is active.}
}
\value{
A list of data frames: \code{orphan_children} (\code{Table}, \code{PlotNumber}),
\code{orphan_su} (\code{PlotNumber}, \code{SiteUnit}), and \code{plots_without_vegetation}
(\code{PlotNumber}). Null child plot numbers are excluded, but null SU plot
numbers and blank strings are retained. Results are distinct and ordered.
}
\description{
Translates the read-only queries in \code{V7mdlReportValidation.One2ManyCheck},
\code{SuRecordsWoEnvRecords}, and \code{PlotRecordsWoVegRecords}. Checks use physical
project tables, not the active \code{USysEnv} view: orphan children are checked
project-wide, while plots without vegetation follow the active SU by default.
No rows are repaired or deleted.
}
