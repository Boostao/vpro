% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment-validation.R
\name{vpro_validate_environment_codes}
\alias{vpro_validate_environment_codes}
\title{Find environment codes absent from configured VPRO lists}
\usage{
vpro_validate_environment_codes(
  context,
  reference_path = vpro_bundled_file("extdata", "VLists.db"),
  use_active_su = TRUE
)
}
\arguments{
\item{context}{A VPRO project context with an active project.}

\item{reference_path}{Path to a SQLite database containing
\code{USysTableOfLists}. Defaults to the bundled VLists database.}

\item{use_active_su}{Whether to restrict validation through the active SU when
one is selected. Set to \code{FALSE} to validate the complete active project.}
}
\value{
A list with \code{findings} and \code{skipped} data frames. Findings contain
\code{list_name}, \code{field}, \code{PlotNumber}, and \code{value}. Skipped definitions contain
\code{list_name}, \code{field}, and \code{reason}.
}
\description{
Reproduces \code{V7mdlReportsValidateEnvData.ValidateEnvData} and its field-level
\code{ReportData} checks. Enabled definitions in \code{USysTableOfLists} identify either
one environment field or a numbered field series through \code{ValidateLoops}.
Null and empty environment values are ignored, matching Access; whitespace is
not trimmed.
}
\details{
Matching uses SQLite \code{NOCASE} collation as a portable approximation of Access
database text comparison. The active SU restricts the Env-Admin join by
default, matching the current \code{USysEnv} query. Results are deterministic and
replace Excel automation. Definitions that cannot be checked are returned in
\code{skipped} instead of being hidden behind field-level modal errors.
}
