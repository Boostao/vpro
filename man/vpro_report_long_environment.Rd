% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-long-environment.R
\name{vpro_report_long_environment}
\alias{vpro_report_long_environment}
\title{Return bounded long-environment data for the active site unit}
\usage{
vpro_report_long_environment(
  context,
  reference_path = vpro_bundled_file("extdata", "VLists.db")
)
}
\arguments{
\item{context}{A VPRO project context with an active project and active SU.}

\item{reference_path}{SQLite reference database containing
\code{MasterSiteUnitList}. Defaults to bundled \code{VLists.db}.}
}
\value{
A list with \code{plots}, \code{units}, \code{names}, \code{fields}, and \code{diagnostics}.
\code{plots} has one row per unique SU membership: \code{SiteUnit}, membership
\code{PlotNumber}, \code{status} (\code{ok}, \code{missing_env}, or \code{missing_admin}),
\code{duplicate_membership_count}, and projected report columns. Projected \code{Plot}
and all other report columns are missing for unmatched Env or Admin rows.
\code{units} has one row per site unit with \code{title} and \code{name_status} (\code{ok},
\code{missing_unit_name}, or \code{conflicting_unit_names}). \code{names} contains sorted
candidate-name vectors. \code{fields} gives ordered labels and heading metadata.
\code{diagnostics} counts unique eligible memberships and excess duplicate rows.
Unlike Access's \code{First()} lookup, ambiguous names never select an arbitrary
title; the unit code is used instead. Sorting and orphan labels are deliberate
modernizations, not Excel-layout parity.
}
\description{
Implements the data contract derived from \code{EnvReport}'s live Access query.
It begins with physical active-SU membership, preserves orphan memberships,
and reads physical Env/Admin tables rather than the inner-joined \code{USysEnv}
view. It returns data only: it does not alter source databases, create Excel
files, or change existing report/UI behavior.
}
