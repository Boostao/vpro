# Sync engine placeholder

sync_pull <- function(con, project_id = NULL) {
  stop("sync_pull not implemented yet")
}

sync_push <- function(con, project_id = NULL) {
  stop("sync_push not implemented yet")
}
