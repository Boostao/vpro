Continue the VPRO Access-to-R migration in /home/bruno/Work/Active project/vpro, using /home/bruno/Work/Active project/VPRO_ACCESS/VPro64_forAI as legacy evidence. Work autonomously through implementation, tests, documentation, and verification. Continue across automatic compaction; do not stop merely to ask what to work on next.

First, refresh context. Read AGENTS.md, inspect Git status, the current package APIs and tests, and the relevant Access SaveAsText procedures. Existing changes are uncommitted: preserve them. Treat generated parity matches as candidates, not proof of equivalence. Do not rely on remembered file contents or stale R-console connections.

Work queue: identify and complete up to four untouched, self-contained, deterministic legacy logic slices. Start by assessing V7mdlUtility.SetTo99, V7mdlUtility.PadSpaces and JustR, V7mdlExportCompactNew.JustTheFirstTwoWords, V7mdlExportToR2.NullToPeriod, and V7mdlPlotProfiling.MadMax. These are candidates, not instructions to implement all five: inspect their callers, existing package equivalents, edge cases, and practical value before selecting. Prefer a coherent slice over accumulating trivial wrappers. If these are unsuitable, find similarly bounded pure or read-only logic in the parity inventory.

For each selected slice, implement a callable, testable R API without Shiny-session or global-state dependence. Cite the source module and procedure in nearby documentation; distinguish observed legacy behavior from intentional R differences. Test boundary values, nulls, invalid inputs, and any unusual VBA coercion or whitespace behavior. Update generated documentation and reviewed parity mappings only after examining the actual implementation and tests. Keep changes limited to the selected behavior; do not refactor unrelated code.

Hard boundaries: do not implement succession copying, terrain write migration, destructive vegetation optimization, or speculative historical-version upgrades. Do not alter original Access databases or use the Windows VM for mutating probes unless a disposable-copy procedure is already established and the probe is essential. Do not silently discard source data, guess ambiguous Access behavior, or promote an unreviewed machine match to “complete.” If a slice requires a new data-loss, authorization, identity, audit, or precision policy, leave that slice unresolved with a concise evidence note and move to another safe candidate.

After each slice, run focused tests. At the end, run the package-native suite and a package check if practical; report skips and failures accurately. Do not commit, push, or deploy. Stop after four completed slices, an insurmountable blocker, or eight hours of work—whichever comes first. Finish with a concise handoff listing files changed, source procedures covered, tests run, unresolved questions, and the next safe slice.
